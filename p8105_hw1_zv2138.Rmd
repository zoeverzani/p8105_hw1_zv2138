---
title: "p8105_hw1_zv2138"
output: github_document
---

```{r Setup}
library(tidyverse)
```

Problem 1 
```{r Creating the df}
problem_1_df = tibble(
  random_samp = rnorm(10),
  positive_samp = random_samp > 0,
  vec = vector(mode = 'character', length = 10)
  factor(fac, levels = c("One", "Two", "Three"))
)
```


